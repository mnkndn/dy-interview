export const generateRandomCards = (pool: number[]) => {
  // initialize an array
  // loop through the pool
  // generate random b/w 16
  // exclude numbers already filled from the random pool

  const randomizedCards = [];

  pool.forEach((poolItem) => {});
};
